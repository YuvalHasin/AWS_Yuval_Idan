{
  "name": "scanbook-backend",
  "version": "1.0.0",
  "description": "ScanBook serverless backend",
  "type": "module",
  "scripts": {
    "deploy": "sam deploy --guided",
    "build": "sam build"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.490.0",
    "@aws-sdk/client-dynamodb": "^3.490.0",
    "@aws-sdk/lib-dynamodb": "^3.490.0",
    "uuid": "^9.0.1"
  }
}